apiVersion: apps/v1
kind: Deployment
metadata:
  name: buildbot-helm-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: buildbot-helm-operator
  template:
    metadata:
      labels:
        name: buildbot-helm-operator
    spec:
      serviceAccountName: buildbot-helm-operator
      containers:
        - name: buildbot-helm-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "buildbot-helm-operator"

apiVersion: v1
kind: Service
metadata:
  name: {{ include "buildbot.fullname" . }}
  labels:
    {{- include "buildbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "buildbot.selectorLabels" . | nindent 4 }}

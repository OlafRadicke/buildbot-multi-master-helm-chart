apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "buildbot.fullname" . }}-test-connection"
  labels:
{{ include "buildbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "buildbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
